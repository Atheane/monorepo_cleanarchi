export enum UsingBasicAuthUriPath {
  PROVISION_PHONE = '/:uid/provision/phone',
  PROVISION_CARD = '/:uid/provision/card',
  ONEY_TOKEN_KEYS = '/oneyfr/token_keys',
  PROVISION_PASSWORD = '/:uid/provision/password',
  BAN_USER = '/:uid/ban',
}

export class Identifier {
  static config = Symbol.for('config');
  static eventRepository = Symbol.for('eventRepository');
  static biConnectionService = Symbol.for('biConnectionService');
  static bankConnectionRepository = Symbol.for('bankConnectionRepository');
}

#!/usr/bin/env node
//'use strict';
//const { spawn } = require('child_process');

//if (process.env.USER !== 'vsts') {
  // We dont want a postinstall in Azure CI
  //const args = ['affected:build', '--parallel'];
  //const cmd = spawn('yarn', args);
  //cmd.stdout.pipe(process.stdout);
  //cmd.stderr.pipe(process.stderr);
//}
